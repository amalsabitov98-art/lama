# --- Ядро озвучки ---
# XTTS-v2 для клонирования/синтеза голоса. Тянет torch как зависимость.
# На обычном компе без NVIDIA всё работает на CPU (медленнее).
TTS>=0.22.0

# --- Интерфейс ---
gradio>=4.0.0

# --- Тесты ---
pytest>=7.0.0

# ffmpeg/ffprobe ставятся системно, НЕ через pip:
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
#
# Липсинк (Wav2Lip) ставится отдельно скриптом install.sh —
# см. README, раздел «Липсинк».
